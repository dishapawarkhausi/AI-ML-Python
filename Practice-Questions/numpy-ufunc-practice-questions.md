# NumPy ufunc Practice Questions

## 1. Introduction to Universal Functions (ufuncs)
1. What are NumPy ufuncs, and how do they differ from regular Python functions?
2. Create two NumPy arrays and perform element-wise addition using a ufunc.
3. Generate an array of 10 random numbers and apply the `np.abs()` function.
4. Perform element-wise multiplication on two 1D NumPy arrays using ufunc.
5. Use a NumPy ufunc to compute the square root of an array of numbers.

## 2. Mathematical Functions
6. Create an array of numbers from 0 to 10 and compute their sine values using `np.sin()`.
7. Generate a 5-element array of random values and compute their natural logarithm.
8. Compute the exponentials of a given NumPy array using `np.exp()`.
9. Create an array and round off its values to 2 decimal places using `np.round()`.
10. Compute the absolute difference between two NumPy arrays.

## 3. Aggregate Functions using ufuncs
11. Create a NumPy array and compute its sum using `np.sum()`.
12. Find the cumulative sum of elements in a NumPy array using `np.cumsum()`.
13. Compute the product of all elements in an array using `np.prod()`.
14. Generate an array and find the minimum and maximum values using ufuncs.
15. Compute the mean, median, and standard deviation of an array using ufuncs.

## 4. Trigonometric and Hyperbolic Functions
16. Create an array of angles in degrees and convert them to radians using `np.radians()`.
17. Compute the tangent values of an array of angles using `np.tan()`.
18. Generate an array and apply `np.sinh()` and `np.cosh()` to compute hyperbolic values.
19. Find the inverse sine of an array of values between -1 and 1.
20. Create an array of angles and compute their arc cosine using `np.arccos()`.

## 5. Custom ufuncs
21. Define a custom universal function to calculate the square of each element in an array.
22. Create a custom ufunc that adds two numbers and returns their sum.
23. Implement a universal function to compute the factorial of an array of numbers.
24. Write a custom ufunc to determine if a number is prime.
25. Create a custom ufunc to find the greatest common divisor (GCD) of two numbers.

## 6. Logical and Comparison ufuncs
26. Create two NumPy arrays and compare them element-wise using `np.greater()`.
27. Find all even numbers in an array using `np.mod()` and a ufunc.
28. Generate a NumPy array of random values and find elements greater than 0.5.
29. Use `np.where()` with a ufunc to replace negative values in an array with zero.
30. Apply `np.logical_and()` to filter numbers that are both even and greater than 10.

## 7. Bitwise and Logical Operations
31. Perform bitwise AND and OR operations on two NumPy arrays.
32. Create an array of binary numbers and apply `np.bitwise_not()` to invert them.
33. Use `np.left_shift()` and `np.right_shift()` to manipulate bitwise operations.
34. Implement `np.logical_or()` to filter values in an array.
35. Apply `np.logical_xor()` on two boolean arrays.

## 8. Broadcasting and Vectorization with ufuncs
36. Create a 3x3 matrix and add a scalar value to all elements using a ufunc.
37. Multiply a 1D array with a 2D matrix using broadcasting.
38. Perform element-wise division on two arrays of different shapes using broadcasting.
39. Compute the power of each element in an array using `np.power()`.
40. Add two NumPy arrays of different shapes using broadcasting.

## 9. Clipping and Rounding Operations
41. Generate an array of random numbers and clip values below 2 and above 8.
42. Use `np.floor()` and `np.ceil()` to round values in an array.
43. Round an array of float values to the nearest integer using `np.rint()`.
44. Create an array with decimal values and truncate it using `np.trunc()`.
45. Generate an array of random numbers and replace all values greater than 5 with 5.

## 10. Real-World Applications
46. Normalize an array using `np.linalg.norm()`.
47. Implement a ufunc to compute moving averages in a dataset.
48. Use `np.histogram()` to analyze the distribution of values in an array.
49. Simulate data transformation in machine learning using ufuncs.
50. Apply ufuncs to clean and preprocess a dataset before feeding it into an AI model.

---

This markdown file contains **50 hands-on NumPy ufunc practice questions** for **coding practice in AI/ML**. Try solving them using NumPy! 🚀
